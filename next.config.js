/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

const config = {
  experimental: {
    serverActions: true,
  },
};
module.exports = nextConfig
module.exports = config